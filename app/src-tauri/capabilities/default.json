{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The pill listens for two events and calls nothing. That is the whole surface it is given — see the contract at the top of app/ui/pill.js.",
  "windows": ["pill"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
