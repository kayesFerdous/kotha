# Rust
target/
**/target/

# Model weights — 778 MB, fetched by setup.sh, never committed
models/

# Audio samples used for the spike
samples/
*.wav
*.mp3
*.m4a

# Node / Tauri frontend (from Phase 4 onward)
node_modules/
dist/
.vite/

# Local scratch
.DS_Store
*.log
out/

# Spike scratch output
spike/gate_out.txt
spike/mel_dump.bin
spike/matched_baseline.json
spike/decode_393.txt
__pycache__/

# Visual C++ runtime DLLs, copied in by the Windows CI leg for bundling. See
# the "Windows runtime DLLs" step in .github/workflows/release.yml.
app/src-tauri/windows-runtime/
*.pyc

# Session handoff notes — PLAN.md is the durable copy
HANDOFF-PROMPT.md

# Tauri's generated schemas
app/src-tauri/gen/

# Pre-rewrite backup of the full history and the working notes.
# Kept out of the repository on purpose: the point of the rewrite was to get
# these off GitHub. Delete it once you are sure you do not want them back.
.backup/

# Working notes. Deliberately not in the repository: Kotha ships as a finished
# product, and these are the workshop floor. Kept on disk, backed up in
# .backup/.
CLAUDE.md
PLAN.md

# Skills installed by `npx skills add`. Tooling, not product.
.agents/
.claude/skills/
skills-lock.json
